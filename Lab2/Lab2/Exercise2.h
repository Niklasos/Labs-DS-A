#include <stdio.h>
#include <stdlib.h>


void Merge(int* arr, int p, int q, int r);
//Enter an array "arr" that you want to sort from the index p to the index r
void MergeSort(int* arr, int startIndex, int endIndex);